const tsNode = require("ts-node")
tsNode.register({
    transpileOnly: true
})

const path = require("path")

const sequelizeRc = {
    config: path.resolve(__dirname, "src", "configs", "db-config.ts"),
    "migrations-path": path.resolve(__dirname, "src", "migrations")
}

module.exports = sequelizeRc